document.addEventListener('DOMContentLoaded',()=>{const menu=document.querySelector('.menu-toggle');const nav=document.querySelector('.nav-links');if(menu&&nav)menu.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('.password-toggle').forEach(button=>button.addEventListener('click',()=>{const input=button.previousElementSibling;input.type=input.type==='password'?'text':'password';button.textContent=input.type==='password'?'Show':'Hide'}));document.querySelectorAll('[data-auth-form]').forEach(form=>form.addEventListener('submit',event=>{event.preventDefault();const button=form.querySelector('button[type=submit]');button.innerHTML='You’re in! <span>→</span>';setTimeout(()=>window.location.href='dashboard.html',600)}));document.querySelectorAll('[data-demo-form]').forEach(form=>form.addEventListener('submit',event=>{event.preventDefault();form.innerHTML='<strong>Thanks for subscribing — stay curious!</strong>'}));const editor=document.querySelector('[data-editor-form]');const publish=document.querySelector('[data-publish]');const publishStory=event=>{if(event)event.preventDefault();if(editor&&editor.checkValidity()){alert('Your story is ready to publish!');editor.reset()}};if(editor)editor.addEventListener('submit',publishStory);if(publish)publish.addEventListener('click',publishStory)});
